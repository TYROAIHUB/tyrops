// CalendarEvent and Calendar types are used as plain objects in JavaScript.
// No runtime exports needed - this file exists for reference.
//
// CalendarEvent shape:
//   { id, title, date, time, duration, type, attendees, location, color, description? }
//   type: "meeting" | "event" | "personal" | "task" | "reminder"
//
// Calendar shape:
//   { id, name, color, visible, type }
//   type: "personal" | "work" | "shared"
